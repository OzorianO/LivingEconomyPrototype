# Стан середовища

Перевірено 17.09.2026 у Windows:

- Папка проєкту: `C:\Users\victus16\Documents\ChatGPT\My Game`.
- Git доступний із середовища агента; наявний локальний репозиторій.
- `dotnet` доступний; перелік SDK ще потрібно перевірити до створення C#-проєкту.
- Unity Hub, Editor та Visual Studio 2022 у перевірених стандартних каталогах
  не знайдено. Це не виключає встановлення на іншому диску або за іншим шляхом.

Наступний крок: визначити наявний Unity Editor і його точну версію.
Якщо Unity ще немає, підготувати встановлення через Unity Hub.
Версію рушія, шаблон і пакети обираємо після цього; актуальні рекомендації
перевіряємо за офіційними джерелами перед встановленням.

Unity-проєкт планується у підпапці `LivingEconomyPrototype` цього репозиторію.
Не відкривати корінь як уже готовий Unity-проєкт: Assets/Packages/ProjectSettings
ще не створені. GitHub Desktop не є обов’язковою умовою роботи з Git.
